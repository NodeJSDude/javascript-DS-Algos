// O(n) Operation -- pass number n -- always a straight line (x = y)
function logItems(n) {
  for (let i = 0; i < n; i++) {
    // increment until it reaches n times
    console.log(i); // output to console each iteration
  }
}

logItems(10); // run 10 times
